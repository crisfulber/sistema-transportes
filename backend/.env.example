# Configurações do Servidor
PORT=3000

# Chave secreta para JWT (IMPORTANTE: Mude isso em produção!)
# Gere uma chave aleatória segura: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=sua_chave_secreta_super_segura_aqui_mude_em_producao

# URL de conexão do PostgreSQL
# Formato: postgresql://usuario:senha@host:porta/nome_banco
# Desenvolvimento local (sem senha):
DATABASE_URL=postgresql://postgres@localhost:5432/sistema_transportes
# Ou com usuário e senha:
# DATABASE_URL=postgresql://usuario:senha@localhost:5432/sistema_transportes

# Ambiente (development ou production)
NODE_ENV=development
